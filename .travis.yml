language: c
compiler:
  - gcc
# Change this to your needs
install: autoreconf --install && automake --add-missing --copy
script: ./configure && make && make check
notifications:
  email:
    - arjenderijke@gmail.com
